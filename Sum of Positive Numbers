fun main() {
    val numbers = listOf(-5, 10, -2, 4, 0, 6, -52, 7, -88)
    var sum = 0
    for (num in numbers) {
        if (num > 0) {
            sum += num
        }
    }
    println("Sum of positive numbers: $sum")
}
